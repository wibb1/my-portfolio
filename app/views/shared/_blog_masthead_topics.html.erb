<div class="nav-scroller py-1 mb-2">
  <nav class="nav d-flex justify-content-center">
    <% @header_topics.each do |topic| %>
      <%=link_to topic.title, topic_path(topic.id), class:"p-2 text-muted" %>
    <% end %>
  </nav>
</div>
