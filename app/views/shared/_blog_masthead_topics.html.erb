<div class="nav-scroller py-1 mb-2">
  <nav class="nav d-flex justify-content-center">
    <% Topic.with_blogs.each do |topic| %>
      <a class="p-2 text-muted" href="<%=topic_path(topic.id)%>"><%=topic.title%></a>
    <% end %>
  </nav>
</div>
