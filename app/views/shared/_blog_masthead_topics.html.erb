<!-- Subject Links - switch from exising to topics -->
<div class="nav-scroller py-1 mb-2">
  <nav class="nav d-flex justify-content-center">
    <p class="p-2 text-muted">Latest Comments:</p>
    <a class="p-2 text-muted" href="#">Comment on Blog</a>
    <p class="p-2 text-muted">Topics:</p>

    <% @topics.each do |topic| %>
      <a class="p-2 text-muted" href="#"><%=topic.title%></a>
    <% end %>

  </nav>
</div>